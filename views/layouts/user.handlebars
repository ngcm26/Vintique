<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Vintique</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="/css/styles.css" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="stylesheet" href="/assets/css/users/usernavbar.css">
  {{#if (eq activePage 'register')}}
    <link rel="stylesheet" href="/assets/css/users/register.css">
  {{/if}}
</head>
<body>
  <!-- Navbar for guests and users -->
  <nav class="navbar navbar-expand-lg user-navbar">
    <div class="container-fluid px-4">
      <div class="d-flex w-100 align-items-center justify-content-between">
        <a class="navbar-brand d-flex align-items-center" href="/">
          <img src="/assets/logo.png" alt="Vintique Logo" class="logo-img">
        </a>
        <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item"><a class="nav-link {{#if (eq activePage 'home')}}active{{/if}}" href="/">Home</a></li>
            <li class="nav-item"><a class="nav-link {{#if (eq activePage 'shop')}}active{{/if}}" href="#">Shop</a></li>
            {{#if user}}
              <li class="nav-item"><a class="nav-link {{#if (eq activePage 'sell')}}active{{/if}}" href="#">Sell</a></li>
              <li class="nav-item"><a class="nav-link {{#if (eq activePage 'qa')}}active{{/if}}" href="#">Q&amp;A</a></li>
              <li class="nav-item"><a class="nav-link {{#if (eq activePage 'messages')}}active{{/if}}" href="#">Messages</a></li>
            {{/if}}
            <li class="nav-item"><a class="nav-link {{#if (eq activePage 'about')}}active{{/if}}" href="#">About</a></li>
            <li class="nav-item"><a class="nav-link {{#if (eq activePage 'feedback')}}active{{/if}}" href="#">Feedback</a></li>
            {{#if user}}
              <li class="nav-item">
                <a class="nav-link {{#if (eq activePage 'cart')}}active{{/if}}" href="#">
                  <i class="fas fa-shopping-cart"></i>
                </a>
              </li>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle {{#if (eq activePage 'account')}}active{{/if}}" href="#" id="accountDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                  Account
                </a>
                <ul class="dropdown-menu" aria-labelledby="accountDropdown">
                  <li><a class="dropdown-item" href="/order-history">Order History</a></li>
                  <li><a class="dropdown-item" href="/account-settings">Account Settings</a></li>
                  <li><a class="dropdown-item" href="/seller-verification">Seller Verification</a></li>
                  <li><hr class="dropdown-divider"></li>
                  <li><a class="dropdown-item" href="/logout">Log Out</a></li>
                </ul>
              </li>
            {{else}}
              <li class="nav-item d-flex align-items-center gap-2">
                <a class="btn btn-signup me-2" href="/register">Sign Up</a>
                <a class="btn btn-login" href="/login">Login</a>
              </li>
            {{/if}}
          </ul>
        </div>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>
      </div>
    </div>
  </nav>
  <div class="container mt-4">
    {{{body}}}
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 